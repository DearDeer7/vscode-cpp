/**
 * Created by DearDeer on 2021/02/22
 */
#include<bits/stdc++.h>
using namespace std;

// #define DEER 1
#define endl '\n'
typedef long long ll;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

#ifdef DEER
    freopen("in.txt", "r", stdin);
    freopen("out.txt", "w", stdout);
#endif
    
    vector<vector<int>> grid(9, vector<int> (9));
    for(int len = 0; len < 9; ++len) {
        for(int i = 0; i < 9; ++i) {
            int j = 9 - 1 - le
        }
    }
    
    return 0;
}